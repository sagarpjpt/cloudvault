export const metadata = {
  title: "Verify Email | CloudVault",
  description: "Create your CloudVault account",
};

export default function VerifyEmailLayout({ children }) {
    return <>{children}</>
}