export const metadata = {
  title: "Reset-Password | CloudVault",
  description: "Create your CloudVault account",
};

export default function ResetPasswordLayout({ children }) {
    return <>{children}</>
}